an exploration of webgpu and wasm(assemblyscript) 

```npm ci``` (for a clean install) 

commands 
- ```npm start``` (to launch the server)
- ```npm run build``` (to build the wasm.ts files) 

new wasm .ts files need to be added to asconfig.json and will be compiled into a single wasm file
